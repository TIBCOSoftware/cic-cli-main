#!/usr/bin/env node

// if (process.argv.length === 2) {
//   process.argv[2] = 'cicmenu';
// }

require('@oclif/command').run().then(require('@oclif/command/flush')).catch(require('@oclif/errors/handle'));
